services:

  backend:

    build:
      context: ../apps/backend

    ports:
      - "8000:8000"

    volumes:
      - ../data:/app/data

  frontend:

      build:
        context: ../apps/frontend

      ports:
        - "3000:5173"  

      depends_on:
        - backend