name: Build Backend

on:
  push:
    branches:
      - main

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: Docker Build
        run: |
          docker build \
          -t montecarlo-backend \
          ./apps/backend
